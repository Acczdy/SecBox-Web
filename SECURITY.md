# Security Policy

## 支持范围

仅当前 `main` 分支接受安全修复。请先确认问题能在最新提交复现。

## 私下报告

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告，不要在公开 Issue 中发布有效利用代码、真实凭据、回调地址或受影响目标数据。

报告请包含：影响、复现条件、最小复现步骤、建议修复方式，以及你希望使用的署名。维护者会尽快确认并协调修复与披露时间。

## 部署方责任

SecBox-Web 包含具有双重用途的安全能力。部署者必须使用 HTTPS、访问控制和防火墙，妥善保存首次 OOB 密码，并及时更新依赖。因错误公开端口、关闭鉴权或泄露运行时卷造成的问题不属于托管服务承诺。

启用 AI 分析时，部署者还应保护服务端 API 密钥，核实模型服务的数据处理与保留政策，并确保发送的邮件、扫描报告和对话内容处于授权范围内。AI 会话数据库位于运行时目录，应按敏感分析数据管理；不需要 AI 时请保持 `AI_API_KEY` 为空。
